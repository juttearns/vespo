{
  "framework": null,
  "installCommand": "unzip -o *.zip -d . -x '__MACOSX/*' 2>/dev/null; npm install",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
